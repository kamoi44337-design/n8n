# —— 基础安全 ——
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change-me-strong
N8N_ENCRYPTION_KEY=please-generate-a-32-characters-random-key


# —— 外网访问（选填，但强烈建议设置）——
# 使用 Railway 提供的域名或你的自定义域
# 例：your-app-name.up.railway.app 或 n8n.yourdomain.com
N8N_HOST=
N8N_PROTOCOL=https


# —— 邮件通知/重置（如需）——
# For example using SMTP
# N8N_EMAIL_MODE=smtp
# N8N_SMTP_HOST=smtp.example.com
# N8N_SMTP_PORT=465
# N8N_SMTP_USER=user@example.com
# N8N_SMTP_PASS=yourpass
# N8N_SMTP_SENDER="n8n <noreply@example.com>"


# ——（方案 A：PostgreSQL 推荐）——
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=containers-us-west-xxx.railway.app
DB_POSTGRESDB_PORT=xxxxx
DB_POSTGRESDB_DATABASE=railway
DB_POSTGRESDB_USER=postgres
DB_POSTGRESDB_PASSWORD=********


# ——（方案 B：SQLite + Volume 简易）——
# 若使用 Volume，将 N8N_USER_FOLDER 指向挂载路径（如 /data）
# 并注释掉上面的 Postgres 配置，改为：
# DB_TYPE=sqlite
# N8N_USER_FOLDER=/data


# —— 其他常用 ——
N8N_DIAGNOSTICS_ENABLED=false
N8N_METRICS=true
# 允许 iframe 嵌入（按需）
# N8N_EDITOR_BASE_URL=https://n8n.yourdomain.com
